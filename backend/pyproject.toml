[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for Cloudflare Workers"
requires-python = ">=3.13"
dependencies = ["fastapi"]

[dependency-groups]
dev = [
    "workers-py>=1.16.2",
]
